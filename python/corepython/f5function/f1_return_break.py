'''
return - return can only be used inside a function 
        will terminate the entire function ,regardless of whether it is inside a loop (like while or for) or a conditional block (like if or else). 
        will return back to where the fn is called
break statement, only exits the current loop, 
'''