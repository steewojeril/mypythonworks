'''
# Module is simply a python file(.py file)that contains code
# Package is a collection of related modules || it will contains an __init__.py file(this can be empty). 
  else it is only a directory

prefered mtds:
    #from module_name import fn_name/class_name
    #import module_name as alias
    #import module_name  || used when module is large
not prefered:
    #from module_name import *
    

'''


